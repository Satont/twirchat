# Issues — user-aliases

(none yet)

## 2026-04-12
- `bun run fix` and `bun run lint` succeeded after wiring alias lookup in `ChatList.vue`.
- `bun test packages/desktop/tests/` still has unrelated existing failures in `useEmoteCache.test.ts` and `useHotkeys.test.ts`.
