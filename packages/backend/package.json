{
  "name": "@twirchat/backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch --no-clear-screen src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsgo --noEmit",
    "test": "bun test tests/",
    "build:prod": "bun run typecheck && bun build --compile --minify --sourcemap src/index.ts --outfile=.out/backend"
  },
  "dependencies": {
    "@twirchat/shared": "workspace:*",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:",
    "@types/node": "catalog:"
  }
}
